# ActorAni V2.0 使用说明

ActorAni V2.0 是一款用于 After Effects 2023 中文版的可停靠脚本面板。

它不会整理或生成预合成内部的动画内容。用户需要先手动把各段角色动画排列在预合成时间线上，插件只负责：

- 创建带持续范围的合成标记；
- 读取预合成中的动画标记；
- 把标记手动匹配到八种方向和状态；
- 根据主合成中预合成图层的位置关键帧，自动切换并循环播放相应动画范围。

## 安装

将 `ActorAniV2.0.jsx` 复制到 AE 2023 的 ScriptUI Panels 文件夹，例如：

```text
C:\Program Files\Adobe\Adobe After Effects 2023\Support Files\Scripts\ScriptUI Panels\
```

重新启动 AE，然后从菜单 `窗口` 中打开 `ActorAni V2.0`。

也可以先通过 `文件 > 脚本 > 运行脚本文件` 临时运行，但放入 ScriptUI Panels 文件夹后才能作为可停靠面板使用。

## 第一步：手动准备预合成动画

进入角色预合成，手动把待机、行走等动画依次排列在时间线上。

这一步完全由用户完成，ActorAni 不会移动、裁剪或生成预合成内的任何图层。

## 第二步：添加动画范围标记

1. 在预合成中，把时间指示器移动到某段动画的起点。
2. 点击面板中的 `添加标记`。
3. 输入动画名称并确认。
4. 插件会在当前时间创建一个默认约 1 秒的合成范围标记；靠近合成末尾时会自动缩短。
5. 根据实际动画长度，在 AE 时间线上调整该标记的起点和持续范围。
6. 对全部动画重复以上步骤。

标记名称必须唯一。ActorAni 不会覆盖当前时间已经存在的合成标记。

插件创建的是“合成标记”，不是某个图层上的图层标记。

## 第三步：读取并匹配动画

1. 返回主合成。
2. 只选中一个需要控制的预合成图层。
3. 点击 `读取/刷新标记`。
4. 在八个下拉框中完成匹配：

   - `正_左_待机`
   - `正_右_待机`
   - `背_左_待机`
   - `背_右_待机`
   - `正_左_走`
   - `正_右_走`
   - `背_左_走`
   - `背_右_走`

同一个动画标记可以同时匹配给多个状态。例如角色没有单独的左右待机动画时，可以把同一个待机标记匹配给两个方向。

## 第四步：应用并制作位移动画

完成八项匹配后，点击 `应用到选中的预合成图层`。

ActorAni 会：

- 给该预合成图层启用时间重映射；
- 添加自动切换表达式；
- 把八项匹配信息保存在该图层的注释中，关闭并重新打开工程后仍可恢复。

之后给这个预合成图层的 `位置` 属性添加关键帧即可。

## 方向和状态规则

每两个相邻的位置关键帧构成一个判断区间：

- X 坐标减小：左；
- X 坐标增大：右；
- Y 坐标减小，也就是向画面上方移动：背；
- Y 坐标增大，也就是向画面下方移动：正；
- 两个位置关键帧的坐标相同：待机；
- 移动结束或进入待机区间后：继承最近一次有效移动的正/背和左/右方向；
- 没有任何可继承方向时：默认使用 `正_左_待机`；
- 纯水平移动会继承之前的正/背方向；
- 纯垂直移动会继承之前的左/右方向。

例如：

- `(1000, 700)` 移动到 `(700, 400)`：X 减小、Y 减小，因此播放 `背_左_走`；
- 下一对关键帧位置相同：播放 `背_左_待机`；
- 最后一个位置关键帧之后：继续使用最后方向的待机动画。

插件按照相邻关键帧的起点和终点判断方向，不分析两个关键帧之间贝塞尔运动路径的局部转向。

## 动画范围循环

每种状态只会在匹配标记的持续范围内循环。

例如某个行走标记从 2 秒开始，持续 0.8 秒，那么使用该状态期间只会循环播放预合成的 2.0–2.8 秒范围，不会进入相邻动画。

切换到新的关键帧区间时，对应动画会从该标记范围的开头重新播放。

## 什么时候需要刷新或重新应用

- 只调整现有标记的起点或持续范围：表达式会直接读取最新范围，不需要重新应用。
- 新增、删除或重命名标记：点击 `读取/刷新标记`，重新检查八项匹配并应用。
- 更换所选预合成图层：必须重新读取。
- 修改位置关键帧：不需要刷新或重新应用。

## 校验与注意事项

- 八种状态必须全部匹配后才能应用。
- 标记名称必须唯一；同名标记会导致应用被阻止。
- 持续时间为 0 的普通点标记不会出现在下拉列表中。
- 每次只处理主合成中选中的一个预合成图层。
- 支持普通位置关键帧，也兼容位置属性的维度分离关键帧。
- 如果图层原本已经启用了非 ActorAni 的时间重映射，应用前会要求确认。
- 八项映射保存在图层注释的 `[[ActorAniV2:BEGIN]]` 数据块中。插件会保留数据块之外的用户注释。
- 如果手动删除或修改 ActorAni 数据块，面板可能无法自动恢复之前的下拉匹配，但已生成表达式仍会继续工作。

## 卸载

关闭 AE，删除 ScriptUI Panels 文件夹中的 `ActorAniV2.0.jsx`，然后重新启动 AE。

已经应用到工程图层上的表达式和时间重映射不会因为删除脚本文件而自动消失，需要在工程中手动关闭或删除。
